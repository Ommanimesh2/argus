# ARGUS Agents — LangGraph pipeline + APIs (Claude + OpenAI)

# LangGraph & state machine (align with StateGraph, END, MemorySaver)
langgraph
langchain-core

# Tracing
langsmith

# LLM providers
anthropic
openai

# API server
fastapi
uvicorn[standard]

# Data validation & env
pydantic
python-dotenv

# Async (used by LangGraph / FastAPI)
aiohttp
